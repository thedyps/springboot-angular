import { RegGoodsValidatorDirective } from './reg-goods-validator.directive';

describe('RegGoodsValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RegGoodsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
